let defaultState = {
    banners: [
        { id: 1, name: 'twenty one pilots' },
        { id: 2, name: 'Oasis' },
    ]
}

const reducer = (state = defaultState) => {
    return state
}

export default reducer